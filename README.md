# Network Monitoring Dashboard - React UI

واجهة المستخدم الحديثة لنظام مراقبة الشبكة الداخلية.

## 🎯 المميزات

✅ **Dashboard** - لوحة تحكم رئيسية بأحدث الإحصائيات  
✅ **Devices** - إدارة وعرض جميع أجهزة الشبكة  
✅ **Alerts** - تتبع ومراقبة جميع التنبيهات  
✅ **Network** - حالة الشبكة العامة والأداء  
✅ **Reports** - توليد وتصدير التقارير  
✅ **Settings** - إعدادات النظام  
✅ **Dark Mode** - وضع ليلي كامل  
✅ **Responsive** - تصميم متجاوب على جميع الأجهزة  

## 📋 المتطلبات

- **Node.js** >= 18.x
- **npm** >= 9.x

## 🚀 البدء السريع

### 1. تثبيت المتطلبات

```bash
npm install
```

### 2. التشغيل في بيئة التطوير

```bash
npm run dev
```

سيفتح المتصفح تلقائياً على `http://localhost:5173`

### 3. بناء الإصدار الإنتاجي

```bash
npm run build
```

### 4. معاينة الإصدار الإنتاجي

```bash
npm run preview
```

## 📁 بنية المشروع

```
src/
├── components/
│   ├── layout/
│   │   ├── MainLayout.tsx    (التخطيط الرئيسي)
│   │   ├── Header.tsx        (رأس الصفحة)
│   │   └── Sidebar.tsx       (الشريط الجانبي)
│   └── common/
│       ├── Card.tsx          (بطاقة أساسية)
│       ├── StatusBadge.tsx   (شارة الحالة)
│       └── StatCard.tsx      (بطاقة الإحصائية)
├── pages/
│   ├── Dashboard.tsx         (الصفحة الرئيسية)
│   ├── Devices.tsx          (صفحة الأجهزة)
│   ├── Alerts.tsx           (صفحة التنبيهات)
│   ├── Network.tsx          (صفحة الشبكة)
│   ├── Reports.tsx          (صفحة التقارير)
│   └── Settings.tsx         (صفحة الإعدادات)
├── styles/
│   └── globals.css          (أنماط عامة)
├── App.tsx                  (مكون التطبيق الرئيسي)
└── main.tsx                 (نقطة البداية)
```

## 🎨 نظام التصميم

### الألوان

```
🟢 Success:  #10b981
🟠 Warning: #f59e0b
🔴 Danger:  #ef4444
🔵 Info:    #3b82f6
```

### Dark Mode

Dark Mode مفعّل افتراضياً ويعمل تلقائياً بناءً على تفضيل النظام.

يمكنك التبديل من خلال:
- زر القمر/الشمس في رأس الصفحة
- سيتم حفظ التفضيل محلياً

## 🔧 المكتبات الرئيسية

- **React 18** - مكتبة واجهة المستخدم
- **TypeScript** - لغة البرمجة المكتوبة
- **Vite** - أداة البناء السريعة
- **Tailwind CSS** - أنماط الويب
- **Recharts** - الرسوم البيانية
- **Lucide React** - الأيقونات
- **React Router** - التنقل
- **Zustand** - إدارة الحالة (مستقبلاً)
- **Axios** - طلبات HTTP

## 🌐 الربط مع Backend

الواجهة جاهزة للربط مع Backend:

### متغيرات البيئة

```env
VITE_API_BASE_URL=http://localhost:3000/api
```

### أمثلة على الربط

```typescript
// استيراد Axios
import axios from 'axios'

// إنشاء مثيل API
const api = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
})

// استدعاء API
async function fetchDevices() {
  const response = await api.get('/devices')
  return response.data
}
```

## 📊 الصفحات الرئيسية

### Dashboard
- إحصائيات فورية
- رسوم بيانية للأداء
- قائمة التنبيهات الأخيرة
- توزيع حالة الأجهزة

### Devices
- جدول جميع الأجهزة
- بحث وفلترة
- عرض تفاصيل الجهاز
- إجراءات سريعة (Edit, Delete)

### Alerts
- قائمة جميع التنبيهات
- فلترة حسب الحالة
- معلومات مفصلة
- إجراءات (Acknowledge, Resolve)

### Network
- حالة الإنترنت
- سرعات التنزيل/التحميل
- أداء الشبكة
- القطاعات الشبكية

### Reports
- توليد التقارير
- تحديد الفترة
- تصدير PDF/Excel
- قائمة التقارير السابقة

### Settings
- إعدادات المراقبة
- إعدادات التنبيهات
- تفضيلات النظام
- معلومات النسخة

## 🔌 APIs المتوقعة

```
GET  /api/devices              - قائمة الأجهزة
GET  /api/devices/:id          - تفاصيل جهاز
POST /api/devices              - إضافة جهاز
PUT  /api/devices/:id          - تحديث جهاز
DELETE /api/devices/:id        - حذف جهاز

GET  /api/alerts               - قائمة التنبيهات
GET  /api/alerts/:id           - تفاصيل تنبيه
PUT  /api/alerts/:id/status    - تحديث حالة التنبيه

GET  /api/network/status       - حالة الشبكة
GET  /api/metrics              - المقاييس والإحصائيات

GET  /api/reports              - قائمة التقارير
POST /api/reports/generate     - توليد تقرير جديد
```

## 🧪 التطوير

### إضافة صفحة جديدة

1. أنشئ ملف في `src/pages/NewPage.tsx`
2. أضف مسار في `src/App.tsx`
3. أضف العنصر في `src/components/layout/Sidebar.tsx`

```typescript
// src/pages/NewPage.tsx
export default function NewPage() {
  return (
    <div className="space-y-6">
      <h1 className="text-3xl font-bold">New Page</h1>
      {/* محتوى الصفحة */}
    </div>
  )
}
```

### إضافة مكون جديد

1. أنشئ ملف في `src/components/common/NewComponent.tsx`
2. استخدمه في الصفحات

```typescript
// src/components/common/NewComponent.tsx
export default function NewComponent() {
  return <div>Component content</div>
}
```

## 🌙 Dark Mode

Dark Mode يعمل تلقائياً:

```typescript
// اكتشاف التفضيل
const isDark = localStorage.getItem('theme') === 'dark' || 
               window.matchMedia('(prefers-color-scheme: dark)').matches

// التبديل
document.documentElement.classList.toggle('dark')
localStorage.setItem('theme', isDark ? 'dark' : 'light')
```

## 📱 Responsive Design

جميع الصفحات تدعم:
- 📱 الهواتف الذكية (320px+)
- 📱 الأجهزة اللوحية (768px+)
- 💻 أجهزة الكمبيوتر (1024px+)
- 📊 الشاشات الكبيرة (1440px+)

## ✨ نصائح التطوير

1. **استخدم TypeScript** - للكود الآمن من الأخطاء
2. **اتبع نمط المجلدات** - للتنظيم والصيانة
3. **استخدم Tailwind** - لتطبيق الأنماط بسرعة
4. **اختبر Dark Mode** - تأكد التوافقية
5. **فعّل Responsive** - اختبر على أجهزة مختلفة

## 🚀 الإطلاق

### للإنتاج

```bash
npm run build
# سيتم إنشاء مجلد dist/
```

### النشر

```bash
# نسخ محتوى dist/ إلى الخادم
# أو استخدم Vercel, Netlify, GitHub Pages

# مثال Vercel:
npm i -g vercel
vercel
```

## 📞 الدعم

للمساعدة والأسئلة:
- راجع الملفات التوثيقية (UI-*.md)
- اطلب مساعدة من الفريق
- اختبر مع المستخدمين

## 📄 الرخصة

MIT License

---

**آخر تحديث**: 2025-12-16  
**الإصدار**: 1.0.0  
**الحالة**: جاهز للتطوير 🚀
